data/pairs/
data/cdx_cache/
data/mine_wayback_log.csv
data/*.log
data/drift_image_cache/

# generated review page(s) — regenerate via scripts/build_eyeball_review.py
evals/eyeball_review*.html

# python
__pycache__/
*.pyc

# secrets — never commit real keys, .env.example documents the shape
.env

# adjudicate/ run logs
adjudicate/logs/

# console/ generated data (regenerate via scripts/build_console_data.py) --
# same rationale as evals/eyeball_review.html: derived from already-gitignored
# sources (data/pairs/, adjudicate/logs/), and the screenshot copies would
# otherwise bloat the repo the same way data/pairs/ would.
console/public/data/
console/public/screenshots/

# node
node_modules/
console/dist/

