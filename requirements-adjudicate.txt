# Extra dependencies for adjudicate/ (the escalation graph). Install after
# requirements.txt. Uses google-genai (the current SDK) -- NOT the
# deprecated google-generativeai package.
#
#   pip install -r requirements.txt
#   pip install -r requirements-adjudicate.txt
#
# Also needs a GOOGLE_API_KEY -- see .env.example.

langgraph==1.2.11
google-genai==2.19.0
python-dotenv==1.2.3
PyYAML==6.0.3
pydantic==2.13.4
